

// Deprecated: portal-specific firebase-config.
// Use `js/shared/firebase-config.js` instead and include it before portal scripts.
if (typeof firebaseConfig === 'undefined') {
  console.warn('js/shared/firebase-config.js not loaded. Include it before portal scripts.');
}

